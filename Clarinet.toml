[project]
name = 'Smart-Contract-Public-Concrete-and-Masonry-Contractor-Management'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.concrete-licensing]
path = 'contracts/concrete-licensing.clar'
clarity_version = 2
epoch = 2.5

[contracts.concrete-quality]
path = 'contracts/concrete-quality.clar'
clarity_version = 2
epoch = 2.5
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
